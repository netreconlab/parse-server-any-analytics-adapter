function _typeof(obj) {
  if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") {
    _typeof = function (obj) {
      return typeof obj;
    };
  } else {
    _typeof = function (obj) {
      return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
  }

  return _typeof(obj);
}

var u = 'undefined';
function hasLocalStorage() {
  return false;

  try {
    if ((typeof localStorage === "undefined" ? "undefined" : _typeof(localStorage)) === u || (typeof JSON === "undefined" ? "undefined" : _typeof(JSON)) === u) {
      return false;
    } // test for safari private


    localStorage.setItem('_' + u, '1');
    localStorage.removeItem('_' + u);
  } catch (err) {
    return false;
  }

  return true;
}

export { hasLocalStorage };
